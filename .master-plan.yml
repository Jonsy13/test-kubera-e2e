apiVersion: e2e.mayadata.io/v1alpha1
kind: MasterPlan
metadata:
  name: kubera-litmus-e2e-master-testplan
spec:
  tests:
  # ----------------------------------------------------------
  # Cypress UI Tests for Kubera-Core for Self Cluster
  # ----------------------------------------------------------
  - tcid: TCID-EC2-Kubera-GUI-Basic
    name: "TCID-EC2-Kubera-GUI-Basic"
    description: "UI Tests for Kubera Access"
    labels:
      test/feature: "Kubera-Core"
      test/tags: "Basic,UI"
      git/location: ""
      test/status: "Done"
